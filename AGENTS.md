# AGENTS.md

本文件用于指导 AI 编码代理在本仓库中的工作方式。  
AI 代理在读取代码、修改文件、执行命令、提交变更或生成说明时，应优先遵循本文件中的约定。

如果本文件与其他项目文档存在冲突，建议按以下优先级处理：

1. 本文件 `AGENTS.md`
2. 项目根目录中的 `README.md`
3. 各模块目录下的局部说明文件
4. 代码注释和现有实现风格

---

## 1. 项目概述

本项目是一个用于存放项目说明、开发规范和 AI 代理协作约定的代码仓库。

主要目标包括：

- 明确项目结构和开发规范
- 统一 AI 代理在本仓库中的行为边界
- 提高代码修改、审查和自动化任务的一致性
- 避免 AI 代理误改关键配置、敏感文件或核心架构

AI 代理应始终从项目整体目标出发，避免只解决局部问题而破坏全局一致性。

---

## 2. 仓库结构

本仓库采用分层结构，不同类型文件应放置到对应目录中。
